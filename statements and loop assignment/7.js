// Q.7 - Take value of length and breadth and then check whether it is square or rectangle. also print it.

let length =10,breadth =15;

if(length === breadth){
    console.log(`This is square`);
}else if(length === length && breadth === breadth){
    console.log(`This is reactangle`);

}

// output:
// This is reactangle